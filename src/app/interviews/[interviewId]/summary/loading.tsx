import PageSkeleton from "@/components/PageSkeleton";

export default function InterviewSummaryLoading() {
  return <PageSkeleton eyebrow="Interview Summary" title="Loading interview summary" rows={6} />;
}
