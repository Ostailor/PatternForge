import PageSkeleton from "@/components/PageSkeleton";

export default function AdminAnalyticsLoading() {
  return <PageSkeleton eyebrow="Admin Analytics" title="Loading product events" rows={5} />;
}
